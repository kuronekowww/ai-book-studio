# Task Plan: AI 讲书知识与文稿工作台

## Goal

交付一个本地可运行的 React + FastAPI + SQLite 网页产品，用《圆圈正义》验证从书籍导入、章节确认、知识拆解、单书内容项目、声音稿生产到 Obsidian 同步的完整闭环。

## Current Phase

Phase 24: 项目级分环节模型选择与专辑选题增强

## Phases

### Phase 1: 实施规划与工程初始化

- [x] 用户批准设计规格
- [x] 创建新 Git 项目并提交设计规格
- [x] 写入实施计划与持续记录文件
- [x] 初始化前端工程
- **Status:** complete

### Phase 2: 后端领域模型与书籍入库

- [x] 建立 FastAPI、SQLite 和初始化机制
- [x] 实现 TXT、Markdown、EPUB 导入
- [x] 实现字面量换行恢复、标题树、脚注和稳定 ID
- [x] 实现章节检查与确认接口
- [x] 使用《圆圈正义》验证 8 个主题和 49 篇文章
- **Status:** complete

### Phase 3: 工作流、模型适配与版本系统

- [x] 建立提示词注册表和模型供应商适配接口
- [x] 保存六个 Dify 工作流迁移源并实现对应执行阶段
- [x] 实现持久运行、取消、重启恢复和单条重跑
- [x] 实现拆书、思维导图、专辑大纲、细纲、初稿和终稿
- [x] 提供确定性演示模型，确保无 API Key 也能验收界面
- **Status:** complete

### Phase 4: React 产品界面

- [x] 书籍库与导入
- [x] 章节切分检查器
- [x] 书籍知识工作台
- [x] 内容项目与专辑大纲检查点
- [x] 声音生产、证据、版本和重跑
- [x] 设置、运行记录和响应式体验
- **Status:** complete

### Phase 5: Obsidian 同步

- [x] 实现目录和 Markdown 渲染
- [x] 实现稳定链接、同步清单和内容哈希
- [x] 实现个人批注保留区块
- [x] 验证重复同步幂等
- **Status:** complete

### Phase 6: 集成验证与修复

- [x] 后端单元与工作流测试
- [x] 前端构建、渲染和 lint
- [x] 《圆圈正义》端到端验收
- [x] 单条重跑和 Obsidian 同步验收
- [x] 最终全量复测与 Git 检查
- **Status:** complete

### Phase 7: 本地交付

- [x] 完成本地安装、配置和启动说明
- [x] 检查不含原书、密钥和运行数据
- [x] 提交实现
- **Status:** complete

### Phase 8: Notion 审核台实施规划

- [x] 确认三栏审核台视觉方案
- [x] 确认 5 条声音并行、单条三阶段顺序
- [x] 确认人工编辑版本规则
- [x] 写入并提交设计规格
- [x] 写入详细实施计划
- **Status:** complete

### Phase 9: 批量生产与人工版本

- [x] 扩展 SQLite 运行与版本字段
- [x] 实现专辑级批次和最多 5 条并行
- [x] 实现失败隔离、去重和重启恢复
- [x] 实现人工终稿版本接口
- [x] 补充后端回归测试
- **Status:** complete

### Phase 10: Notion 风格三栏审核台

- [x] 重构全局视觉变量与基础组件
- [x] 实现目录与批次进度左栏
- [x] 实现终稿主编辑区和折叠辅助稿
- [x] 实现证据与版本右栏
- [x] 实现响应式折叠
- **Status:** complete

### Phase 11: 端到端验收

- [x] 通过 lint、构建和全部测试
- [x] 用《圆圈正义》验证批量终稿生产
- [x] 验证人工编辑版本与失败单条重跑
- [x] 验证 Git 不包含原书、密钥和运行数据
- **Status:** complete

### Phase 12: 本地交付

- [x] 更新 README 与变更说明
- [x] 提交本次实现
- [x] 保留可用的本地预览
- **Status:** complete

### Phase 13: DeepGate Anthropic 接入

- [x] 确认 Messages 协议和 DeepGate 兼容策略
- [x] 写入并提交设计规格
- [x] 写入实施计划
- [x] 实现 Anthropic 供应商
- [x] 增加模拟请求测试
- **Status:** complete

### Phase 14: 本机配置与真实验证

- [x] 将实际配置写入 Git 忽略的 `.env`
- [x] 重启后端并确认设置状态
- [x] 执行一次最小真实请求
- [x] 完成全部回归测试和密钥边界检查
- [x] 提交不含密钥的实现
- **Status:** complete

### Phase 15: 单条声音上下文链设计

- [x] 检查现有声音三阶段输入链
- [x] 确认三个阶段的上下文范围
- [x] 比较实现方案并确认设计
- [x] 写入、复核并提交设计规格
- [x] 用户审核书面规格
- **Status:** complete

### Phase 16: 单条声音上下文链实施

- [x] 写入详细实施计划
- [x] 实现结构化阶段输入
- [x] 迁移声音细纲提示词
- [x] 增加输入快照与回归测试
- [x] 使用真实样例验证并提交
- **Status:** complete

### Phase 17: 逐章模型拆书设计

- [x] 定位《当代中国社会分层》实际失败原因
- [x] 确认章节输入与批次失败策略
- [x] 确认长拆书稿压缩与下游生成时机
- [x] 比较实现方案
- [x] 确认完整设计
- [x] 写入、复核并提交设计规格
- [x] 用户审核书面规格
- **Status:** complete

### Phase 18: 逐章模型拆书实施

- [x] 写入详细实施计划
- [x] 迁移非叙事类逐章拆书提示词
- [x] 实现章节级并发、解析、版本与失败重跑
- [x] 实现拆书稿压缩、思维导图和专辑大纲生成
- [x] 更新知识资产与进度界面
- [x] 使用目标书籍验证并提交
- **Status:** complete

### Phase 19: 全局模型切换实施

- [x] 用户确认全局切换范围、即时生效和运行任务锁定规则
- [x] 写入、复核并提交设计规格
- [x] 写入详细实施计划
- [x] 实现模型预设目录、端点规范化和本地选择持久化
- [x] 实现设置 API 与任务级 provider 快照
- [x] 实现设置页模型切换交互
- [x] 完成后端、前端、构建与密钥边界验证
- [x] 更新说明并提交实现
- **Status:** complete

### Phase 20: 段落级证据溯源实施

- [x] 用户确认多片段来源、章节层级、严格校验和旧数据策略
- [x] 写入、复核并提交设计规格
- [x] 写入详细实施计划
- [x] 实现原文片段、版本集和知识资产关联迁移
- [x] 实现段落切分、稳定索引与旧书片段初始化
- [x] 升级逐章拆书输出、严格校验和事务保存
- [x] 升级专辑按知识资产编排与声音证据上下文
- [x] 升级页面、证据 API 和 Obsidian 双向链接
- [x] 完成回归、本地迁移和页面连接验证
- **Status:** complete

### Phase 21: Doubao Seed 2.0 Pro 模型预设

- [x] 确认完整 DeepGate 地址和 OpenAI 兼容协议
- [x] 写入、复核并提交设计规格
- [x] 写入实施计划
- [x] 增加模型预设和地址契约测试
- [x] 更新设置接口断言与使用说明
- [x] 完成全量验证并提交
- **Status:** complete

### Phase 22: 逐章拆书结构化输出容错

- [x] 定位豆包批量失败的错误分布和模型行为
- [x] 确认部分成功保存与下游阻断策略
- [x] 写入、复核并提交设计规格
- [x] 写入详细实施计划
- [x] 加固 OpenAI 兼容结构化输出
- [x] 实现逐项校验、部分章节版本和诊断信息
- [x] 更新运行记录与章节页面
- [x] 完成全量测试
- [x] 使用《当代中国社会分层》整本试跑
- **Status:** complete

### Phase 23: 取消金句逐字校验与历史章节升级

- [x] 确认金句不再执行逐字原文匹配
- [x] 写入、复核并提交设计规格
- [x] 写入实施计划
- [x] 更新校验器、提示词和测试
- [x] 实现无模型历史章节重新校验入口
- [x] 升级《当代中国社会分层》最新部分章节
- [x] 完成全量验证并提交
- **Status:** complete

### Phase 24: 分环节模型选择与专辑选题增强

- [x] 确认全局默认与项目级分环节覆盖规则
- [x] 确认专辑大纲默认 Kimi K3
- [x] 确认辅助 JSON 修复和压缩任务不单独显示
- [x] 写入、复核并提交设计规格
- [x] 实现书籍与项目模型设置存储、迁移及 API
- [x] 实现六类任务的模型解析与运行快照
- [x] 升级专辑大纲选题提示词
- [x] 实现书籍拆书与项目五个创作环节模型配置
- [x] 完成后端、前端和本地服务验证
- [x] 提交实现
- **Status:** complete

### Phase 25: 面向未读听众的有声专辑大纲

- [x] 确认轻松但克制的表达强度
- [x] 写入、复核并提交设计规格
- [x] 升级专辑大纲提示词与提示词版本
- [x] 更新演示供应商和结构校验
- [x] 补充提示词与工作流定向测试
- [x] 完成后端与前端全量回归测试
- [x] 完成差异复核并提交
- **Status:** complete

### Phase 26: 版本化提示词配置中心

- [x] 确认全局默认与项目级覆盖
- [x] 确认只开放用户提示词模板
- [x] 确认保存时校验必要占位符
- [x] 确认保留历史版本
- [x] 确认全局与项目配置统一放在左侧“提示词”标签页
- [x] 设计模板、版本、绑定、占位符与任务快照
- [x] 写入、复核并提交设计规格
- [x] 用户审核设计规格
- [x] 写入实施计划
- [x] 创建数据库迁移和提示词服务
- [x] 增加版本、继承、校验、渲染与预览 API
- [x] 将专辑大纲和声音三阶段切换为有效提示词快照
- [x] 将批量运行和单条重跑锁定提示词版本
- [x] 新增左侧“提示词”三栏配置页面
- [x] 增加后端服务、API、工作流和前端结构测试
- [x] 完成差异复核与提交
- **Status:** complete

## Key Questions

1. 如何在无真实 API Key 的自动化环境中完成可重复验收？使用确定性演示供应商，真实供应商走同一接口。
2. 如何兼顾首版单书界面和未来多书项目？数据库使用多对多关系，首版交互限制选择一本书。
3. 如何保护 Obsidian 人工批注？系统区块和保留区块分离，合并时只更新系统区块。
4. 如何避免章节重命名导致引用失效？所有关系使用稳定 UUID，标题只作为显示字段。

## Decisions Made

| Decision | Rationale |
|----------|-----------|
| React + TypeScript + Vite | 清晰的本地网页前端，便于以后独立部署 |
| FastAPI + SQLite | 适合文本处理、本地任务与未来数据库替换 |
| 确定性演示模型 + 可插拔真实模型 | 无密钥可完整演示，有密钥可运行真实流程 |
| 知识库与创作库分离 | 支持知识复用和未来多书融合 |
| 原书不进入 Git | 保护版权和本地隐私 |
| 本地交付，不使用云端托管 | 符合用户已确认的首版形态 |
| Notion 式三栏审核台 | 终稿是主要审核对象，同时保留目录、证据和版本上下文 |
| 单条串行、跨声音最多 5 条并行 | 保证阶段依赖并提高整张专辑生产效率 |
| 人工修改保存为新版本 | 保留模型原稿和完整审校历史 |
| 原生 httpx 调用 Anthropic Messages | 无需新增 SDK，并可显式关闭代理继承 |
| 系统要求合并进 user 消息 | 适配 DeepGate 不稳定传递顶层 system 内容的行为 |
| 独立上下文组装器 | 保证批量、单条和重跑共享同一套来源校验与输入格式 |
| 人物关系按原文块提取并由服务端挂 ID | 避免模型生成或混用错误来源标识 |
| 专辑集数与特殊要求采用可选用户输入 | 不用固定数量扭曲书籍内容结构；无输入时由模型按拆书稿自行决定 |
| 过渡声音由模型按需增加 | 不作为强制项，也不用于凑集数 |
| 章节拆书与专辑大纲采用严格结构化输出 | 便于校验来源索引、派生知识资产、失败重跑和版本追溯 |
| 思维导图与专辑大纲共享拆书稿并行生成 | 避免两条链使用不同事实输入，同时缩短等待时间 |
| 模型采用全局默认与项目级分环节覆盖 | 保留全局切换便利性，同时允许不同生产环节按项目选择更合适的模型 |
| 切换只影响新任务 | 运行任务锁定 provider 快照，保证多阶段产物模型一致 |
| 专辑大纲默认 Kimi K3 | 强化专辑选题和标题表现，用户仍可在项目中切换 |
| 章节与段落证据采用双层结构 | 一级章节继续控制调用边界，段落块负责精确引用 |
| 每项知识资产独立引用多个片段 | 支持跨段观点、论据和案例，同时保留原书顺序 |
| 专辑按知识资产而非片段编排 | 避免段落数量直接膨胀为声音数量 |
| 提示词采用系统、全局、项目三类不可变版本 | 兼顾安全约束升级、用户历史恢复和任务运行稳定性 |
| 提示词运行时解析为不可变快照 | 与模型快照一致，避免批量运行中途漂移 |

## Errors Encountered

| Error | Attempt | Resolution |
|-------|---------|------------|
| 在尚未创建的目录中运行初始化命令导致工作目录不存在 | 1 | 先从父目录创建目录，再进入新目录初始化 |
| 视觉伴随脚本没有执行权限 | 1 | 使用 `bash script` 调用 |
| 沙箱禁止监听本地端口 | 2 | 按权限流程获批后在本地启动 |
| 标准站点初始化器检测到项目中已有设计文档 | 1 | 临时移出文档，完成一次标准初始化后原样恢复 |
| npm 在沙箱中无法写入用户缓存日志 | 1 | 获批后完成依赖安装 |
| 开发服务器在沙箱中无法监听调试端口 | 1 | 获批后启动本地预览 |
| 系统 Python 的 `pydantic_core` 架构不兼容当前 Mac | 1 | 为项目创建独立虚拟环境并安装原生依赖 |
| x86_64 Homebrew Node 令统一启动脚本通过 Rosetta 选择了错误 Python 架构 | 1 | Apple Silicon 上显式以 arm64 Python 模块方式启动 Uvicorn |
| pip 在沙箱中无法访问依赖索引 | 1 | 获批后在项目虚拟环境安装依赖 |
| React lint 禁止在 effect 中同步触发状态更新 | 1 | 改为异步加载回调，并移除不必要的派生状态 effect |
| 旧数据库缺少 `parent_run_id` 时新索引先于字段升级创建 | 1 | 将索引创建移动到幂等字段迁移之后 |
| 独立烟雾测试只 `source .env`，变量未导出给 Python | 1 | 使用 `set -a` 自动导出后再加载配置 |
| 设计规格自检修订补丁上下文未匹配 | 1 | 缩小补丁上下文并按实际段落完成修订 |
| 实施计划误写为 `backend/.venv` | 1 | 按现有启动脚本改用项目根目录 `.venv` |
| 拆书方法改为异步后旧测试仍同步调用 | 1 | 更新工作流测试为 `asyncio.run` 并补充叙事/非叙事分支 |
| 最终 API 回归时此前本地服务已退出 | 1 | 重新启动统一开发服务后继续迁移与接口验收 |
| 提示词补丁因既有文案上下文不同未匹配 | 1 | 读取实际提示词后使用更小补丁完成迁移 |
| 在后端子目录使用项目相对路径导致编译命令找不到文件与虚拟环境 | 1 | 回到项目根目录执行相同检查 |
| 专辑提示词字符串引号不配对导致 Python 编译失败 | 1 | 修正字符串结尾并校验全部提示词可格式化 |
| 章节候选测试字符串乘法优先级制造了重复标题 | 1 | 对重复正文片段加括号，确保测试输入只有三个一级标题 |
| 首次真实序言拆书在沙箱内无法连接 DeepGate | 1 | 按权限流程在受控网络环境重试 |
| 真实模型原文引号未转义导致 JSON 解析失败 | 1 | 增加严格转义要求和仅在语法失败时触发的 JSON 修复调用 |
| 沙箱阻止本机 8000 端口只读回归 | 1 | 按权限流程完成本地 API 状态检查 |
| 在 `backend` 工作目录重复使用 `backend/app/...` 路径，导致检查命令找不到文件 | 1 | 改用当前目录相对路径 `app/...` 后继续检查 |
| 片段无损校验把切句后新增的段间空白视为正文变化 | 1 | 无损字符校验改为移除全部空白后比较，严格文本匹配仍保留独立规范化规则 |
| 旧批次快照测试只预期解析一个模型，新结构会分别解析三个阶段 | 1 | 更新测试契约为三阶段模型快照，并补充阶段 provider 断言 |
| 在 backend 子目录运行根目录 `.venv/bin/pytest` 导致路径不存在 | 1 | 回到项目根目录运行完整测试 |
| 沙箱阻止统一开发服务监听 8000/3000/9229 端口 | 1 | 按权限流程在受控本地环境启动 `npm run studio` |
| 浏览器自动化策略禁止检查 localhost 页面 DOM | 1 | 不绕过策略，改用本地 API、前端构建和结构测试验证；页面保留给用户直接刷新查看 |
| 本轮检索误用不存在的 `frontend/src` 路径 | 1 | 后续先读取前端目录结构，再使用实际路径 |
| 首次测试补丁把两个测试文件的上下文合在同一更新块 | 1 | 拆分为按文件、按函数的小补丁 |
| 新增测试在实现前按预期失败 3 项 | 1 | 补齐提示词、演示输出和结构校验后定向测试 3 项通过 |
| 首次工作流接入补丁包含多个重复调用上下文，未能整体匹配 | 1 | 拆分为导入、构造器、专辑调用和声音调用的小补丁 |
| 首次批次补丁同时修改多个元数据返回位置，末尾上下文不匹配 | 1 | 分别修改创建、执行和汇总函数 |
| 批次无提示词锁时仍传空的新关键字，旧测试替身不兼容 | 1 | 仅在存在非空提示词快照时传入新参数 |
| 提示词页面 effect 中同步设置载入状态触发 React lint | 1 | 移除 effect 起始同步状态更新，仅在异步回调中更新 |
| lint 仍把 effect 中直接调用 `load()` 视为潜在同步更新 | 2 | 通过 Promise 微任务启动加载，使全部状态更新发生在异步回调 |
| 本地运行态烟雾测试发现 3000/8000 服务均未启动 | 1 | 按权限流程启动统一开发服务后重新验证 |
| 统一启动发现已有 3000/8000 服务并启动了临时 3001 前端 | 1 | 立即停止临时副本，保留并验证已有服务 |

## Notes

- 在重大技术决策前重读本文件和 `findings.md`。
- 外部文档和模型输出只写入 `findings.md`，不写入本计划。
- 每完成一个阶段，更新状态和 `progress.md`。
