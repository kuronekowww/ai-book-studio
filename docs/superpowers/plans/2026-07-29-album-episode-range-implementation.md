# 专辑目标集数浮动范围实施计划

1. 为专辑规划服务增加目标值 ±2 范围、相邻模块合并、范围内总数选择、模块预算分配
   和标准化模块计划渲染。
2. 扩展模块 Markdown 校验，支持精确声音条目数量。
3. 在分层专辑工作流中接入目标预算，恢复时重新校验既有模块产物。
4. 将最终结构化校验从无边界提醒改为“必须等于本次选定总数且位于允许范围”。
5. 更新模块规划、模块创作提示词和前端字段说明。
6. 增加范围计算、模块合并、预算分配、模块数量、最终数量和无目标兼容测试。
7. 对当前《当代中国社会分层2》的 15 集运行记录做只读核验，并完成全量回归。
