# Progress Log

## Session: 2026-07-29

### Phase 27: 示例字幕风格研究与四阶段提示词优化

- **Status:** complete
- Actions taken:
  - 接收六篇“硬核干货”类字幕作为目标风格参考。
  - 确认本轮先做风格研究和提示词设计，不直接修改产品配置。
  - 开始检查项目现有四阶段提示词与版本化配置能力。
  - 读取六篇字幕开场、结尾和高频听觉路标，归纳出认知缺口、极简模型、因果链、纠偏与回扣结构。
  - 获取“城乡分野”两个段落级原文块，划定样稿事实和数据边界。
  - 确认样稿主题为《当代中国社会分层》“城乡分野为何如此顽固”，并核对 1 个观点、1 个案例、3 条论据和 1 条金句的来源索引。
  - 比较只改终稿、四阶段分工和每次附完整范文三种方案，用户批准四阶段分工方案。
  - 完成四组可直接粘贴到提示词配置页的用户模板，使用的占位符均在现有白名单内。
  - 完成约 1500 字口播样稿，以“城墙—门—锁”作为解释模型，事实范围限定在两个指定原文块。
  - 写入并自检完整设计文档，未发现占位符、留白或 Markdown 差异问题。

## Session: 2026-07-24

### Phase 1: 实施规划与工程初始化

- **Status:** complete
- **Started:** 2026-07-24
- Actions taken:
  - 完成需求文档、SOP、六个 Dify YAML 和《圆圈正义》结构检查。
  - 完成架构、页面、数据模型、Obsidian 和验收标准设计确认。
  - 创建独立 Git 项目。
  - 写入并提交设计规格。
  - 创建持续实施计划、发现记录和进度日志。
  - 使用 Sites 标准模板初始化 vinext/Vite 工程并完成依赖安装。
  - 启动 `http://localhost:3000/` 本地预览。
- Files created/modified:
  - `.gitignore`
  - `docs/superpowers/specs/2026-07-24-ai-book-studio-design.md`
  - `task_plan.md`
  - `findings.md`
  - `progress.md`

### Phase 2: 后端领域模型与书籍入库

- **Status:** complete
- Actions taken:
  - 检查站点结构和本机 Python 依赖。
  - 确认系统 Pydantic 二进制架构不兼容，决定创建项目私有虚拟环境。
  - 建立 FastAPI、SQLite、书籍/章节/知识/项目/声音/版本实体。
  - 实现 Markdown、TXT、EPUB 解析和章节确认接口。
  - 《圆圈正义》真实导入准确识别 8 个主题、49 篇文章和 4 个长文语义分段。
- Files created/modified:
  - `backend/app/config.py`
  - `backend/app/db.py`
  - `backend/app/ingestion.py`
  - `backend/app/main.py`

### Phase 3-5: 工作流、前端与 Obsidian

- **Status:** complete
- Actions taken:
  - 实现演示与 OpenAI 兼容模型供应商。
  - 建立提示词版本、持久运行、声音版本和重跑。
  - 完成书籍库、章节编辑、知识工作台、项目大纲编辑和声音工作台。
  - 保存六个原始 Dify YAML 作为迁移源。
  - 实现 Obsidian 增量同步、同步清单和批注保留。
- Files created/modified:
  - `app/page.tsx`
  - `app/globals.css`
  - `backend/app/providers.py`
  - `backend/app/prompts.py`
  - `backend/app/workflows.py`
  - `backend/app/obsidian.py`
  - `workflows/dify-source/*.yml`

### Phase 6: 集成验证与修复

- **Status:** complete
- Actions taken:
  - 后端 4 项测试通过。
  - 前端 lint、构建和服务器渲染测试通过。
  - 《圆圈正义》端到端生成与 Obsidian 幂等性通过。
  - 修复 x86_64 Homebrew Node 与 arm64 Python 虚拟环境混用导致的 Rosetta 启动问题。
  - 使用 `npm run studio` 统一启动并确认网页、API 和已有样例数据均正常。
- Files created/modified:
  - `backend/tests/`
  - `tests/rendered-html.test.mjs`
  - `scripts/dev-backend.sh`

### Phase 7: 本地交付

- **Status:** complete
- Actions taken:
  - 完成安装、环境配置、统一启动和验收说明。
  - 确认原书、SQLite 运行数据、虚拟环境与密钥均未纳入 Git。
  - 完成全量复测并提交首版本地产品。

### Phase 8: Notion 审核台实施规划

- **Status:** complete
- **Started:** 2026-07-24
- Actions taken:
  - 阅读用户提供的 Notion 设计上下文并提取工作区视觉规则。
  - 通过视觉伴随比较三种终稿审核布局，用户选择三栏审核台。
  - 确认专辑大纲后手动触发批量生产。
  - 确认单条三阶段串行、不同声音最多 5 条并行。
  - 确认失败隔离继续生产和人工终稿新版本规则。
  - 写入并提交 `2026-07-24-notion-batch-review-design.md`。
  - 写入详细实施计划，进入批量生产与人工版本实现阶段。
  - 第一轮数据库迁移测试发现旧表建索引顺序问题，已改为先补字段再创建索引。

### Phase 9: 批量生产与人工版本

- **Status:** complete
- Actions taken:
  - 为运行记录增加父子关系、失败阶段、顺序和进度元数据。
  - 为产物版本增加模型/人工来源标记，并实现旧 SQLite 幂等升级。
  - 实现项目级批次、最多 5 条声音并行和单条三阶段顺序。
  - 实现失败隔离、活动批次去重、重启恢复和失败后单条重跑归并。
  - 实现人工终稿新版本接口。

### Phase 10: Notion 风格三栏审核台

- **Status:** complete
- Actions taken:
  - 全站切换为暖白、浅灰、克制紫色的 Notion 工作区视觉。
  - 左栏加入批次进度和声音阶段状态。
  - 中栏将终稿放大为可编辑主区域，细纲与初稿默认折叠。
  - 右栏加入原文证据和模型/人工版本记录。
  - 加入未保存保护和响应式布局。

### Phase 11: 端到端验收

- **Status:** complete
- Actions taken:
  - 后端 7 项测试通过，覆盖并发上限、失败隔离、旧库升级和人工版本。
  - 前端 lint、生产构建和 2 项渲染/结构测试通过。
  - 《圆圈正义》批次跳过已有 2 条，成功生成剩余 10 条终稿。
  - 保存一条人工终稿 v2，并确认模型终稿 v1 保留。

### Phase 12: 本地交付

- **Status:** complete
- Actions taken:
  - 更新 README 中的批量生产和三栏审核说明。
  - 完成 Git 范围、密钥、原书和运行数据检查。
  - 提交实现并保留本地前后端预览。

### Phase 13: DeepGate Anthropic 接入

- **Status:** complete
- **Started:** 2026-07-24
- Actions taken:
  - 确认 DeepGate 使用 Anthropic Messages 协议。
  - 确认关闭环境代理并将完整提示要求合并到 user 消息。
  - 写入并提交不含访问密钥的接入设计规格。
  - 写入实施计划并完成 Anthropic 供应商适配。
  - 增加模拟请求测试，校验请求地址、鉴权头、消息结构和响应解析。
  - 首次独立烟雾测试因 `.env` 变量未导出而回落到演示供应商；已定位为 shell 导出方式问题。

### Phase 14: 本机配置与真实验证

- **Status:** complete
- Actions taken:
  - 将实际 DeepGate 配置写入 Git 忽略的本机 `.env`。
  - 重启统一开发服务，确认后端报告 Anthropic 供应商和目标模型已就绪。
  - 通过显式导出环境变量完成一次最小真实调用，获得非空模型响应。
  - 后端 9 项测试、前端 2 项测试、lint 和生产构建全部通过。
  - 确认 `.env` 未被 Git 跟踪，提交范围不包含访问密钥。

### Phase 15: 单条声音上下文链设计

- **Status:** in_progress
- **Started:** 2026-07-24
- Actions taken:
  - 检查现有提示词注册表、声音生成服务和原始 Dify 声音细纲工作流。
  - 确认当前细纲阶段只收到原文块，未收到专辑大纲中的当前声音框架。
  - 确认当前初稿只收到细纲，终稿只收到初稿，两个阶段都没有再次附带原文块。
  - 用户确认三阶段统一采用“上一步产物 + 对应原文块”的输入规则。
  - 检查现有专辑大纲数据结构，确认每条声音目前只有标题、内容类型、风格和来源小节 ID。
  - 用户确认新增可编辑的多行声音框架，并在确认专辑大纲时一起审核。
  - 用户确认书籍上传时选择叙事类或非叙事类，仅叙事类提示词包含人物关系。
  - 用户确认叙事类人物关系在拆书阶段由模型提取，按原文块 ID 存为知识资产，声音细纲按关联块筛选。
  - 比较结构化字段、JSON 元数据和运行时推断三种实现，用户确认采用结构化字段与知识资产方案。
  - 写入单条声音上下文链设计规格，覆盖数据、拆书、审核、三阶段输入、错误处理和验收标准。
  - 完成规格自检，移除让模型生成来源 ID 的歧义，并明确超长输入由供应商显式失败而不静默截断。
  - 用户于 2026-07-27 审核规格并批准实施。

### Phase 16: 单条声音上下文链实施

- **Status:** in_progress
- **Started:** 2026-07-27
- Actions taken:
  - 恢复项目计划、最近提交和现有前后端实现。
  - 确认数据库迁移集中在 `MIGRATION_COLUMNS`，可为既有数据库幂等增加书籍类型和声音框架。
  - 确认上传接口使用 multipart 表单，专辑大纲更新会删除并重建声音记录。
  - 确认当前拆书接口和服务是同步流程，而模型供应商是异步接口，人物关系模型提取需要调整该边界。
  - 检查演示供应商、现有工作流测试、前端上传状态和结构测试，确定可在不依赖真实模型的情况下完整回归。
  - 写入详细实施计划，分为数据库、上下文、叙事拆书、生成链、前端和交付六个任务。
  - 首次运行迁移测试发现虚拟环境路径写错，已按项目启动脚本修正为根目录 `.venv`。
  - 为书籍、声音和产物版本增加 `book_type`、`content_framework`、`input_snapshot`，旧库迁移测试 3 项通过。
  - 首轮全量后端测试定位到旧工作流测试未等待异步拆书，已更新调用方式并补充人物关系分支测试。
  - 实现叙事类与非叙事类细纲模板、人物关系提取模板，以及带明确证据边界的初稿和终稿模板。
  - 新增独立上下文组装器，细纲使用框架/匹配关系/原文，初稿和终稿使用最新上一步产物/同一原文。
  - 叙事类拆书最多并行 5 个原文块，成功资产保留，失败块可重试，来源 ID 由服务端写入。
  - 专辑确认增加框架、来源及项目归属校验，所有模型产物保存输入快照。
  - 后端 17 项测试通过，覆盖旧库迁移、上下文分支、人物关系部分失败重试和确认校验。
  - 上传页增加叙事/非叙事类型选择，书籍工作台可修改类型并提示重新拆书。
  - 专辑大纲增加多行声音内容框架、前端即时校验和生产阶段原文说明。
  - 三栏审核台显示当前声音框架，人物关系知识资产在叙事类书籍指标中可见。
  - 前端 lint、生产构建和 2 项结构/渲染测试全部通过。
  - 最终 API 回归发现此前本地服务已退出，准备重新启动统一开发服务验证真实本地数据库迁移。
  - 重新启动统一服务，现有 SQLite 成功增加三个新列，书籍和声音接口正常。
  - 使用当前 DeepGate Anthropic 配置执行短叙事人物关系请求，返回 1 条合法关系 JSON。
  - 验证声音详情 API 返回内容框架但不暴露完整输入快照，前端继续运行于 `http://localhost:3000/`。
  - 更新 README 中的书籍类型、声音框架、人物关系和三阶段原文输入说明。
  - 最终验证 8 个提示词均可安全格式化，后端 17 项、前端 2 项、lint、生产构建和差异检查全部通过。
  - 交付前补充旧项目兼容回填：历史空声音框架根据标题生成可用初值，避免已进入生产的声音无法从细纲重跑。

### Phase 17: 逐章模型拆书设计

- **Status:** in_progress
- **Started:** 2026-07-27
- Actions taken:
  - 用户报告《当代中国社会分层》（李强）拆书失败，并提供非叙事类逐章拆书提示词与目标输出格式。
  - 开始检查目标书籍的章节结构、失败运行和现有拆书实现。
  - 确认目标 EPUB 已成功导入并确认 234 个章节节点，但全书只有 1–3 级标题，解析器全部标记为 `theme`。
  - 确认现有非叙事拆书仅选择 `kind IN ('article', 'section')`，因此实际选中 0 个章节，写入 0 条知识资产，却错误地把书籍标记为 `analyzed`。
  - 确认现有非叙事拆书没有调用模型，也没有建立章节级运行记录；用户提供的逐章提示词尚未迁入实际执行链。
  - 用户确认以序言、第一章至末章、附录等一级章节为一次模型调用，并聚合其全部二、三级标题与正文。
  - 聚合统计显示正文一级章节约 3,591–26,642 字，适合逐章调用；EPUB 另有目录和 4 个表格注释被误识别为一级标题。
  - 确认当前章节审核页只显示和编辑三级、四级节点，无法正确审核该 EPUB 的一、二、三级章节树。
  - 用户确认自动纳入序言、前言、导论、章节、附录和后记，排除目录、版权页、表格注释及过短异常一级标题，并允许人工恢复或排除。
  - 用户确认完整保存每章结构化拆书稿，同时派生概念、金句、主要观点、论据和案例知识卡片。
  - 用户确认最多 5 章并行、单章失败隔离、成功立即保存、失败统一展示并支持单章重跑。
  - 用户补充下游要求：生成专辑大纲时输入全部章节拆书稿；过长时先压缩，并基于同一输入分别生成 Markdown 思维导图和结构化专辑大纲。
  - 用户取消专辑固定 30 条以上的要求，改为可选填写“专辑特殊要求”和“期望集数”；未填写时由模型根据内容量自行决定。
  - 过渡声音不再强制，只在模型判断确有承上启下需要时增加；不能用于凑集数。
  - 用户确认采用严格结构化专辑大纲输出；思维导图与专辑大纲共享完整或逐章压缩后的拆书稿，并行调用模型。
  - 用户确认逐章拆书、章节版本、派生知识卡片和失败重跑设计。
  - 用户报告上传书籍选择文件后页面状态无变化；检查确认当前文件输入未绑定 React 状态，也没有 `onChange` 已选文件反馈。
  - 用户确认上传文件选中/移除/重试反馈、完整章节审核树、章节进度与失败重跑，以及专辑特殊要求和期望集数输入的页面设计。
  - 完成逐章模型拆书与专辑生成设计规格，经过占位、矛盾、歧义、范围和密钥自检后提交 Git。
  - 用户审核并确认书面规格。
  - 当前环境未提供 `writing-plans` 技能，使用已启用的文件化规划能力写入等价的详细实施计划并进入 Phase 18。
  - 完成章节候选字段、章节版本表、知识资产追溯字段和专辑生成参数的幂等数据库迁移。
  - 新增稳定 `content_index`、章节聚合、严格 JSON 校验、Markdown 渲染和五类知识卡片派生模块。
  - 非叙事类拆书已切换为最多五章并行的模型流程，支持单章失败隔离、立即保存和单章重跑。
  - 新增完整拆书稿汇总、超长逐章压缩缓存、思维导图与专辑大纲并行生成及来源校验。
  - 上传文件控件已显示文件名、格式、大小、勾选状态，并支持重新选择、移除和未选择禁用。
  - 章节审核页扩展到一至四级结构与一级纳入开关；项目页新增特殊要求、期望集数和重新生成入口。
  - 真实数据库迁移后，《当代中国社会分层》从错误的 `analyzed` 退回待拆书；30 个一级节点自动纳入 25 个，排除目录和 4 个表格/脚注误标题。
  - 首次真实序言模型输出因英文引号转义错误被安全拒绝，没有写入残缺结果。
  - 增加 JSON 语法修复提示词，只在解析失败时修正转义和括号，并继续执行原始来源索引校验。
  - 第二次真实序言拆书成功，保存章节版本 v1、927 字 Markdown 和 12 张可追溯知识卡片。
  - 浏览器实际选择 README.md 后显示勾选、文件名、格式、4 KB 和已选择状态，按钮启用；移除后状态复原。
  - 本地 `/health`、`/api/books` 和 `/openapi.json` 返回 200，新单章重跑与项目大纲生成接口已注册。
  - 将“仅部分章节已生成、没有当前失败”的状态独立为 `analysis_partial`，避免把未启动章节误显示为失败。
  - 完成密钥、原书、SQLite 和输入快照的 Git 边界检查，并提交逐章拆书与专辑生成实现。

## Test Results

| Test | Input | Expected | Actual | Status |
|------|-------|----------|--------|--------|
| 设计规格自检 | 规格 Markdown | 无占位、范围矛盾和空白要求 | 通过 | ✓ |
| Git 项目初始化 | 新项目目录 | 独立 main 分支和首个提交 | `d8113ad` | ✓ |
| 《圆圈正义》导入 | Markdown 原书 | 8 主题、49 文章 | 8 主题、49 文章、4 语义分段 | ✓ |
| 知识拆解 | 已确认章节 | 观点/案例/金句和思维导图 | 102 条资产、56 行导图 | ✓ |
| 声音生成 | 第一条声音 | 细纲、初稿、终稿 | 三阶段完成 | ✓ |
| 指定节点重跑 | 从初稿开始 | 保留旧版本 | draft v1/v2、final v1/v2 | ✓ |
| Obsidian 幂等 | 同一内容同步两次 | 第二次无变更 | 171 → 0 | ✓ |
| 后端测试 | pytest | 全部通过 | 4 passed | ✓ |
| 前端 lint | ESLint | 无错误 | 通过 | ✓ |
| 前端构建与渲染 | npm test | 构建并渲染产品壳 | 通过 | ✓ |
| 统一启动 | npm run studio | 前端与 API 同时可用 | 3000/8000 均正常 | ✓ |
| 批次并发 | 7 条测试声音 | 同时活跃不超过 5 | 最大活跃数 5 | ✓ |
| 失败隔离 | 1 条模拟失败 | 其余继续并可单条归并 | 6 成功、1 失败、重跑后全成功 | ✓ |
| 《圆圈正义》批次 | 10 条待生成声音 | 一次生成全部终稿 | 10/10 成功，专辑共 12 条 | ✓ |
| 人工终稿版本 | 编辑第 3 条终稿 | 新版本且模型原稿保留 | human v2 + model v1 | ✓ |

## Session: 2026-07-27 — 全局模型切换

### Phase 19: 实施规划

- **Status:** complete
- Actions taken:
  - 检查现有 `.env`、设置状态接口、Anthropic provider 和设置页。
  - 确认当前模型只在进程启动时构造，网页不能切换。
  - 用户确认采用全局模型选择、立即生效，运行任务锁定启动时模型。
  - 比较预设目录、环境变量重启和项目级配置三种方案，采用后端固定预设目录。
  - 写入并提交 `2026-07-27-global-model-switching-design.md`。
  - 写入详细实施计划，分为模型目录、任务快照、设置 API、前端交互和交付验证五个任务。
  - 实现 7 个固定模型预设、Messages 地址规范化和原子化本地选择持久化。
  - 模型状态只公开非敏感字段；损坏配置回退环境变量，未知模型不改变当前选择。
  - 新增模型目录与 provider 测试，8 项通过。
  - 工作流所有真实模型节点改为使用顶层任务传入的 provider 快照，产物记录快照中的实际模型。
  - 批次运行记录持久化 `model_id`，后台恢复通过模型目录重建原 provider；单条声音运行采用相同规则。
  - 设置状态接口扩展模型列表与选择来源，新增全局模型切换接口。
  - 设置页增加 7 模型选择、保存状态、即时生效说明和失败回退。
  - 阶段性验证：后端 30 项、前端 2 项、lint 与生产构建通过。
  - 为无法映射预设的环境变量模型增加可恢复的内部运行快照 ID。
  - 为思维导图增加 provider/model 迁移与写入，旧数据库幂等升级。
  - 运行中的本地 API 返回 7 个模型，默认保持 Claude Sonnet 4.6，未暴露 API Key。
  - 浏览器确认设置页完整显示 7 个选项、当前模型、全局切换按钮与任务边界说明。
  - 最终验证：后端 32 项、前端 2 项、lint、生产构建、差异检查和密钥扫描全部通过。

## Session: 2026-07-27 — 段落级证据溯源

### Phase 20: 实施规划

- **Status:** complete
- Actions taken:
  - 核对现有 `content_index` 实际只指向标题节点正文，子主题下所有知识项共用一个来源。
  - 用户确认保留一级章节切分，章节内部生成 300–800 字语义段落块。
  - 用户确认每项知识资产允许多个有序来源，并执行严格原文匹配。
  - 用户确认旧结果保留但需逐章重跑，不自动推断旧资产的段落来源。
  - 用户确认专辑按知识资产组织，声音再确定性汇总直接证据与辅助上下文。
  - 写入、复核并提交 `2026-07-27-paragraph-evidence-traceability-design.md`。
  - 写入详细实施计划，拆分为迁移、切分、拆书、专辑上下文、界面/Obsidian 和验证六个任务。
  - 新增版本化原文片段集、稳定段落索引、知识资产来源和声音资产关联表。
  - 实现约 300–800 字语义段落切分、无损校验、正文变化新建版本和旧版本保留。
  - 逐章模型输入改为段落索引；概念、观点、论据、金句和案例分别携带来源数组。
  - 提取型内容执行逐字原文匹配，案例必须携带可逐字匹配的证据原句。
  - 章节结果与确定性知识资产 ID 在同一事务写入；旧资产标记失效而不删除。
  - 专辑模型改按知识资产 ID 编排，声音上下文确定性汇总直接证据与相邻辅助上下文。
  - 新增片段、知识资产来源和声音证据 API；书籍页与三栏审核台显示精准来源。
  - Obsidian 新增片段笔记及片段、知识资产、声音文稿双向链接。
  - README 增加新证据链及旧章节逐章重跑说明。
  - 本地 SQLite 完成幂等迁移；当前数据库为空，未触发真实模型调用或整书重跑。
  - 页面确认前后端连接成功。
  - 最终验证：后端 36 项、前端 2 项、lint、生产构建和差异检查全部通过。

## Session: 2026-07-27 — Doubao Seed 2.0 Pro

### Phase 21: 模型预设实施

- **Status:** complete
- Actions taken:
  - 确认完整地址为 `http://deepgate.ximalaya.local/doubao-seed-2.0-pro/api/v1/chat/completions`。
  - 确认采用 OpenAI 兼容协议，并由现有 provider 统一追加 `/chat/completions`。
  - 写入、复核并提交模型预设设计规格。
  - 写入精简实施计划，覆盖模型目录、请求契约、设置接口、文档和验证。
  - 新增第 8 个全局预设 `doubao-seed-2.0-pro`，使用 `openai-compatible` provider。
  - 基础地址保存为 `/api/v1`，模拟请求确认最终调用 `/api/v1/chat/completions`。
  - 设置接口确认公开 8 个模型且不泄露 API Key。
  - README 与 `.env.example` 已增加豆包模型说明。
  - 定向测试 12 项通过；最终后端 38 项、前端 2 项、lint、生产构建和差异检查全部通过。
  - 未自动切换用户当前全局模型。

## Session: 2026-07-27 — 逐章拆书结构化输出容错

### Phase 22: 容错实施

- **Status:** complete
- Actions taken:
  - 核对最近两轮豆包拆书：49 次失败中，24 次字符串未闭合、19 次其他 JSON 语法损坏、6 次逐字原文校验失败。
  - 确认短序言成功，长章节失败输出集中在约 7,000–11,500 字符，符合默认输出上限和结构化输出不稳定特征。
  - 用户确认有效资产部分保存、无效资产列出问题、部分章节阻止进入专辑。
  - 写入、复核并提交容错设计规格。
  - 写入实施计划，覆盖 provider、逐项校验、数据库、工作流、页面和整本真实试跑。
  - OpenAI 兼容结构化任务改为低温度、16K 输出上限和 JSON Object 模式，网关不支持时自动回退。
  - 读取 `finish_reason` 并区分截断、异常结束、超时、连接、HTTP 和无效网关响应；截断不再进入 JSON 修复。
  - JSON 语法只自动修复一次，失败运行记录保存错误类别、响应长度和解析错误摘要。
  - 章节按知识资产逐项校验并保存部分成功版本；无效观点不再拖累合法论据和案例。
  - 用户在真实试跑中确认最终规则：所有资产校验来源索引，仅金句逐字校验；概念、观点、论据、案例和案例证据允许忠实概括。
  - 章节版本新增问题清单与有效/无效数量；部分成功阻止思维导图和专辑大纲。
  - 书籍页展示部分成功、问题清单和单章重跑；运行记录将拆书任务标为“章节任务”。
  - 《当代中国社会分层》整书真实试跑：最新 25 章中 19 章完整成功、6 章部分成功、0 章失败；896 条活动知识资产，剩余 16 个未通过项。
  - 两个真实失败章单独重跑后均完整成功；对剩余 8 个部分章再重跑一轮，其中 2 章转完整成功。
  - 最终验证：后端 42 项、前端 2 项和生产构建全部通过；书籍详情 API 正确返回 25 章、6 个部分成功章和解码后的问题数组。

## Session: 2026-07-28 — 取消金句逐字校验

### Phase 23: 校验规则与历史升级

- **Status:** complete
- Actions taken:
  - 用户确认金句不再校验是否与原文完全一致，但继续要求真实段落级来源索引。
  - 用户确认现有部分成功章节无需调用模型，直接按新规则重新校验并创建成功版本。
  - 写入、复核并提交设计规格，并写入实施计划。
  - 删除金句连续原文匹配，提示词改为优先保留原文但允许轻微整理。
  - 增加本地历史重新校验服务与维护命令，升级前比较知识资产稳定 ID 集合以保护数据。
  - 执行时目标书已有一章被另行重跑成功，实际升级剩余 5 个最新部分版本。
  - 5 章均创建新的本地重新校验成功版本，模型调用为 0，旧部分版本及问题记录保留。
  - 《当代中国社会分层》最新 25 章全部成功，活动知识资产保持 904 条，书籍状态更新为 `analyzed`。
  - 下游完整章节检查返回 25 章且全部成功，无运行中任务。
  - 最终验证：后端 44 项、前端 2 项、lint 和生产构建全部通过。

## Session: 2026-07-28 — 分环节模型选择与专辑选题增强

### Phase 24: 实施

- **Status:** in_progress
- Actions taken:
  - 用户确认采用“全局默认 + 项目级分环节覆盖”。
  - 确认六个可选环节：章节拆书、思维导图、专辑大纲、声音细纲、声音初稿、声音终稿。
  - 确认专辑大纲默认 Kimi K3，其他环节默认跟随全局。
  - 确认 JSON 修复、人物关系提取和长文本压缩继承所属任务模型，不单独显示。
  - 完成并提交分环节模型选择与专辑选题增强设计规格。
  - 当前环境没有 `writing-plans` 技能，使用文件化实施计划作为替代。
  - 核对现有调用链：章节、专辑规划、单条声音和批量生产目前都只锁定一个全局 provider。
  - 确定需要将项目规划拆成思维导图与专辑大纲两个模型快照，将声音生产拆成三个阶段快照。
  - 新增书籍拆书模型和项目五个创作环节模型的持久化字段与统一路由服务。
  - 项目规划已支持思维导图与专辑大纲分别调用模型；声音生成已支持三阶段分别调用模型。
  - 首轮定向测试 28 项通过、1 项旧批次快照断言失败；失败原因是旧测试只预期解析一次模型，新实现按三个阶段恢复快照。
  - 更新批次测试后定向测试 26 项全部通过。
  - 首次全量测试命令误在 backend 子目录使用根目录虚拟环境路径，已记录并改回项目根目录。
  - 前端 lint、构建和 2 项结构测试通过；后端全量 52 项通过。
  - 沙箱内启动统一服务因端口监听权限被拒绝，准备按权限流程在本地环境启动。
  - 确认已有统一服务正在 3000/8000 运行，OpenAPI 已包含书籍与项目模型设置端点。
  - 现有两个内容项目迁移成功：专辑大纲为 Kimi K3，其他创作环节跟随全局 Doubao Seed 2.0 Pro；现有书籍拆书模型均正确跟随全局。
  - 浏览器自动化策略阻止读取 localhost DOM；未尝试绕过，改由 API、构建和静态结构测试覆盖页面验证。
  - 专辑大纲提示词升级为问题导向的总编组稿方法，要求标题具有具体问题意识，并输出核心主题与两至四条核心要点。
  - 运行记录页面增加章节模型和声音三阶段模型快照显示。
  - 真实自动保存接口验证通过：“专辑1”专辑大纲保持 Kimi K3，章节拆书写回跟随全局后仍解析为 Doubao Seed 2.0 Pro。
  - 最终回归：后端 53 项、前端 2 项、lint、生产构建和差异检查全部通过。
  - 本地前端 3000、后端 8000 均返回 200；“专辑1”保留 24 集，五个创作环节模型配置读取正常。
  - 提交实现：`fe65d3d feat: add per-stage model selection`。

## Session: 2026-07-28 — 面向未读听众的有声专辑大纲

### Phase 25: 实施

- **Status:** in_progress
- Actions taken:
  - 用户确认表达强度为“轻松但克制”。
  - 完成并提交面向未读听众的专辑大纲设计规格：`821a430 docs: design audio-first album outlines`。
  - 恢复现有文件化计划并定位提示词、演示供应商、结构校验和现有测试。
  - 首轮检索误用不存在的 `frontend/src`，已记录；本轮核心改动位于后端，后续按实际前端目录执行回归。
  - 确认专辑主要内容原样保存为 `content_framework`，可在不迁移数据库的情况下增加“听众钩子”。
  - 确认现有来源校正逻辑独立于主要内容结构，本轮无需触碰知识资产与原文索引链路。
  - 首次测试补丁因混合了两个测试文件的上下文而未应用；已改为分文件小补丁。
  - 先新增三项定向契约测试，旧实现按预期失败：提示词仍为旧版本、演示输出缺少听众钩子、校验器未执行新结构约束。
  - 专辑大纲提示词升级到 `2026-07-28.3`，加入未读听众、有声连续理解、相邻声音承接、最低必要背景和轻松克制的故事讲述规则。
  - 演示供应商新增“听众钩子”；结构校验新增钩子、字段顺序和 2 至 4 条核心要点检查。
  - 定向测试 3 项全部通过。
  - 后端完整回归 54 项全部通过，Python 编译检查通过；仅保留既有 FastAPI `on_event` 弃用警告。
  - 前端 lint、生产构建和 2 项页面结构测试全部通过。
  - 复核专辑草稿签名已包含提示词版本，旧草稿不会跨版本复用；已有专辑不会自动改写。
  - 差异检查通过，Phase 25 实施完成，准备独立提交后进入提示词配置中心设计。
  - 提交实现：`877f9f3 feat: make album outlines audio-first`。

## Session: 2026-07-28 — 版本化提示词配置中心

### Phase 26: 设计

- **Status:** complete
- Actions taken:
  - 用户要求左侧新增提示词标签页，开放专辑大纲、声音细纲、声音初稿和声音终稿的提示词配置。
  - 用户确认采用全局默认与项目级覆盖，并统一放在新提示词页面。
  - 用户确认只开放用户提示词主体，系统角色和输出结构约束由程序保护。
  - 用户确认保存时校验必要占位符，并保留全部历史版本。
  - 完成版本化模板注册表、占位符契约、继承规则、任务快照、预览与三栏页面设计。
  - 设计自检发现系统默认若只保存在模板表中，无法可靠恢复旧系统版本；已改为系统、全局、项目三类不可变版本并分别锁定系统版本与用户版本。
  - 补充声音阶段预览的声音选择器，消除“当前声音”来源歧义。
  - 用户审核规格并批准开始实施。

### Phase 26: 实施

- **Status:** in_progress
- Actions taken:
  - 当前环境没有 `writing-plans` 技能，按 planning-with-files 流程写入数据库、服务、工作流、API、页面和测试的分阶段计划。
  - 核对数据库迁移、静态提示词、声音上下文和批量模型快照路径，确定新增提示词服务可复用现有 `PromptDefinition` 与 `workflow_runs.metadata_json`。
  - 定位专辑同步生成、单条异步运行、批量重启恢复和前端主导航的具体接入点。
  - 新增提示词模板、版本、项目绑定三张表，以及专辑/声音产物提示词版本引用字段。
  - 新增四个系统默认可编辑模板、受保护约束、必要/可选占位符、保存校验、单次渲染、继承、历史恢复和锁定快照服务。
  - 提示词服务定向测试 4 项全部通过。
  - 首次工作流接入补丁因同一调用在多个分支重复出现而未整体匹配，已改用小范围补丁。
  - 声音上下文增加项目、书籍类型和结构化占位符变量，并补充上一集最新终稿。
  - 专辑生成已改用可配置提示词快照，草稿缓存签名和项目记录保存系统/用户版本 ID。
  - 声音三阶段已改用各自提示词快照，产物保存系统/用户版本 ID 和编译后的输入快照。
  - 上下文、工作流、专辑生成和提示词服务定向回归 17 项全部通过。
  - 首次批次接入补丁因多个相似元数据块上下文不同未整体匹配，已拆分处理。
  - 批次定向测试 14 项中 11 项通过、3 项旧替身签名失败；原因是无锁批次仍传入空 `stage_prompt_locks`，准备改为条件传参。
  - 修复空快照兼容后，模型路由、设置、批次和提示词相关定向测试 14 项全部通过。
  - 新增提示词版本、历史、恢复默认、取消项目覆盖和无模型预览 API。
  - 单条与批量运行元数据均锁定三阶段提示词版本，应用重启恢复沿用同一版本；批次结束保留快照元数据。
  - 新增 API 与批次提示词快照测试，相关 11 项全部通过。
  - 新增左侧提示词入口、范围/项目切换、四环节编辑器、占位符插入、即时校验、预览和历史恢复界面。
  - 首次前端 lint 检出 effect 起始同步 `setBusy`，已移除该同步状态更新。
  - 第二次 lint 仍将 effect 直接调用 `load()` 识别为潜在同步更新，改由 Promise 微任务启动加载。
  - 后端全量 60 项通过；前端 lint、生产构建和 3 项结构测试通过。
  - 本地烟雾测试发现 3000/8000 当前均未启动，准备启动统一开发服务后复测。
  - 统一启动发现已有前后端进程占用 3000/8000，临时前端自动落到 3001；已立即停止临时副本，未终止用户原有服务。
  - 在受控本地环境验证现有 3000/8000 均返回 200；提示词 API 返回四个系统默认模板。
  - “专辑1”四个环节均正确显示系统默认且无项目覆盖，未修改任何现有内容。
  - 最终回归：后端 60 项、前端 3 项、lint、生产构建和差异检查全部通过。
  - 实际项目无模型预览成功：专辑拆书稿预览已填充并截断，受保护约束单独返回。
  - 最终补充跨环节提示词版本防串用校验；提示词服务、API、工作流和批次定向测试 19 项通过。
  - README 增加提示词配置使用说明，并修正旧的逐字原文校验描述。
  - Phase 26 实施完成，准备提交。
| 本次后端测试 | pytest | 全部通过 | 7 passed | ✓ |
| 本次前端测试 | npm test | 构建和结构校验通过 | 2 passed | ✓ |
| Anthropic 模拟请求 | pytest | URL、鉴权、消息与解析正确 | 2 项新增用例通过 | ✓ |
| DeepGate 最小真实请求 | Anthropic Messages | 返回非空内容 | 目标模型返回 5 字符内容 | ✓ |
| DeepGate 接入回归 | pytest / lint / test / build | 全部通过 | 后端 9、前端 2、lint 与构建通过 | ✓ |
| 上下文链后端回归 | pytest | 迁移、拆书、上下文、批次均通过 | 17 passed | ✓ |
| 上下文链前端回归 | lint / test / build | 审核界面与生产构建通过 | 2 passed，lint 与构建通过 | ✓ |
| 叙事人物关系真实请求 | DeepGate Anthropic | 合法关系 JSON | 1 条关系，结构有效 | ✓ |
| 输入快照边界 | 声音详情 API | 框架可见、快照不下发 | 通过 | ✓ |
| 逐章拆书后端回归 | pytest | 聚合、并发、失败重试、版本、索引和专辑生成通过 | 23 passed | ✓ |
| 逐章拆书前端回归 | lint / test / build | 上传状态与新审核界面通过 | 2 passed，lint 与构建通过 | ✓ |
| 目标书迁移 | 《当代中国社会分层》 | 排除目录与 4 个误标题 | 25 纳入、5 排除 | ✓ |
| 真实单章拆书 | 序言 | 合法版本与知识卡片 | v1、927 字、12 卡片 | ✓ |
| 上传状态浏览器验收 | README.md | 选择可见、按钮启用、移除复原 | 通过 | ✓ |

## Error Log

| Timestamp | Error | Attempt | Resolution |
|-----------|-------|---------|------------|
| 2026-07-24 | 在不存在的工作目录中执行初始化命令 | 1 | 从父目录创建项目后重试 |
| 2026-07-24 | 视觉伴随脚本无执行权限 | 1 | 改用 bash 调用 |
| 2026-07-24 | 沙箱拒绝本地监听端口 | 2 | 获批后重新启动 |
| 2026-07-24 | 初始化器拒绝非空项目目录 | 1 | 临时移出文档并在初始化后恢复 |
| 2026-07-24 | npm 无法写入用户缓存日志 | 1 | 获批后完成安装 |
| 2026-07-24 | 开发服务器无法监听调试端口 | 1 | 获批后启动本地预览 |
| 2026-07-24 | 系统 Pydantic Core 为错误 CPU 架构 | 1 | 改用项目私有虚拟环境 |
| 2026-07-24 | 统一启动经 x86_64 Node 派生后端，选中错误 Python 架构 | 1 | Apple Silicon 上显式用 arm64 Python 启动 |
| 2026-07-24 | pip 在沙箱中无法访问依赖索引 | 1 | 获批后安装 |
| 2026-07-24 | React effect 触发同步 setState lint 错误 | 1 | 改为异步回调并去掉派生状态 effect |
| 2026-07-27 | 真实章节首次返回非法 JSON | 1 | 增加语法修复回退，第二次真实请求成功 |
| 2026-07-28 | 检索使用不存在的 `frontend/src` 路径 | 1 | 后续读取实际目录后再执行前端检查 |
| 2026-07-28 | 测试补丁混合两个文件上下文导致匹配失败 | 1 | 拆分为按文件的小补丁 |
| 2026-07-28 | 工作流接入补丁包含多个重复调用上下文 | 1 | 拆分为导入、构造器和各调用点的小补丁 |
| 2026-07-28 | 批次提示词快照补丁末尾元数据上下文不匹配 | 1 | 分别修改创建、执行和汇总函数 |
| 2026-07-28 | 提示词页面 effect 同步设置载入状态触发 lint | 1 | 仅在异步请求回调中更新组件状态 |
| 2026-07-28 | effect 直接调用可能同步更新的 `load()` 再次触发 lint | 2 | 通过 Promise 微任务调用异步加载 |
| 2026-07-28 | 本地前后端端口均未监听 | 1 | 启动 `npm run studio` 后继续运行态验收 |
| 2026-07-28 | 启动时发现已有服务占用端口，临时前端使用 3001 | 1 | 停止临时副本并只读验证已有 3000/8000 服务 |

## 5-Question Reboot Check

| Question | Answer |
|----------|--------|
| Where am I? | 已完成单条声音完整上下文链 |
| Where am I going? | 后续可扩展更多模型供应商和多书融合创作 |
| What's the goal? | 交付可用的本地 AI 讲书知识与文稿工作台 |
| What have I learned? | 见 `findings.md` |
| What have I done? | 已完成产品、真实模型、叙事拆书、三阶段上下文、测试和文档交付 |
