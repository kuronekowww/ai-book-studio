# Findings & Decisions

## Phase 34 Baseline

- 用户在专辑 4 中填写了“15集，每集2000-2500字”，但该自由文本只进入专辑规划；
  目标声音的细纲、初稿和终稿输入快照均未出现 `2000-2500`。
- 当前活动全局声音细纲与初稿模板仍明确要求“约 1500 字”。
- 目标声音跨第四章和第五章，框架要求同时覆盖身份制、单位制、经济分层、中心边缘、
  新阶层、中产阶级和社会保障；声音细纲本身达到 3287 字符。
- 样稿为 1310 字符、约 1146 个汉字、17 段，平均每段约 67 个汉字；DeepSeek V4
  终稿为 3290 字符、约 2907 个汉字、14 段，平均每段约 208 个汉字。
- 同一声音的 Claude 终稿为 3063 字符，DeepSeek V4 终稿为 3290 字符；模型会放大
  篇幅差异，但上游范围过大和字数要求未传递才是主要原因。
- 用户批准项目级统一的最少/最多字数，默认 2000/2500；汉字和数字计数，标点与
  空白不计入，连续英文单词计 1 字。
- 用户批准在初稿和终稿生成后自动验收，超出范围最多校正两次；仍不合格则阶段失败，
  批量任务继续后续声音。
- 参考文风应提炼为可执行规则：单一中心问题、具体认知缺口、日常解释、数据后解释
  意义、短段落、问答和对比、结尾回扣；不复制范文，也不模仿固定口头禅。
- 设计规格已提交：
  `docs/superpowers/specs/2026-07-30-episode-word-range-and-audio-style-design.md`。
- 实施后项目、持久运行、单条声音与整张专辑批次共享同一字数范围快照；刷新或服务重启
  不会退回默认值。
- 初稿和终稿分别独立验收，最多进行两次基于实际偏差的定向扩写或压缩；仍不合格会记录
  明确失败原因，整张专辑继续处理其他声音。
- 四类提示词已发布全局 v2，系统保护规则使用动态范围并压制旧模板中的固定 1500 字；
  用户仍可在“提示词”页继续创建项目级版本。
- 新版服务运行态已确认首页与项目 API 返回 200，10 个既有项目均已补齐 2000/2500。

## Phase 33 Baseline

- 最新运行 `471f0d1b9f5b43f4b8ad9d33d2ce2137` 使用 DeepSeek V4 Pro；
  `MODULE_001` 分配 1 集、`MODULE_002` 分配 2 集，但模型都输出 15 集。
- 当前有效用户模板来自全局 v1，仍包含“把全书拆书稿编排成一张专辑”和
  `期望集数：{{desired_episode_count}}`；系统模板虽已模块化，但不会覆盖用户正文中的
  全书语义。
- 工作流把 `desired_episode_count` 渲染成“目标 15、允许 13 至 17、本次规划 17”，
  同时在 `module_brief` 写入局部 1/2，形成直接冲突。
- 用户批准保留全局提示词历史，通过运行时局部数量隔离和一次内部纠偏解决。
- 回归模拟旧全局模板仍写“全书”和期望集数：模块分配 2 时，首次故意返回 15 集，
  系统自动调用一次纠偏并成功保存 2 集；纠偏再次返回 15 时正确标记失败且不循环。
- 真实任务 `471f0d1b9f5b43f4b8ad9d33d2ce2137` 已在修复热更新前结束为
  `partial_failed`，保留 7 个旧失败模块；系统没有擅自重新调用 DeepSeek。

## Phase 32 Baseline

- 当前运行 `559bbaff9bf742de84a9e46b5dd07209` 已正确保存
  `desired_episode_count=15`，并非前端漏传。
- 该运行的模块规划生成 15 个知识模块，但每个模块仍建议 3 至 5 集，建议集数合计约
  56；根因是全专辑目标没有被转成模块级预算。
- 当前模块校验只检查 Markdown 字段和章节标识，最终校验只生成集数偏差提醒，两层
  都没有阻止超量结果。
- 用户明确否决精确硬约束，批准固定允许目标值上下浮动 2 集；输入 15 时最终允许
  13 至 17 集，未填写时保持模型自由。
- 将当前被取消任务的旧模块计划只读套用新算法：15 个模块的原建议总数约 56，
  新算法选择 17 集并分配为 13 个单集模块和 2 个双集模块，分配总和严格为 17。
- 当前旧任务已由用户取消，因此修复没有重新发起真实模型调用；用户重新点击生成后
  会建立新任务并使用范围预算。

## Phase 31 Baseline

- 已批准设计：`docs/superpowers/specs/2026-07-29-lightweight-album-planning-design.md`。
- 已批准实施计划：`docs/superpowers/plans/2026-07-29-lightweight-album-planning-implementation.md`。
- 当前专辑流程仍由 `WorkflowService.generate_project_knowledge_outputs` 把约 9.3 万字符拆书稿一次交给模型，并要求直接输出含知识资产 ID 的严格 JSON。
- Kimi K3 小输入可用，但当前全书大请求会长时间运行后返回空文本；Claude 同类请求会由网关在 600 秒附近返回 504。
- 新流程必须保持现有思维导图、历史项目、项目级模型覆盖和提示词版本兼容。
- 专辑审核阶段只保存一级章节范围；`episode_knowledge_items` 延迟到声音细纲前的 `match_episode_sources` 阶段写入。
- 开发按已提交的八任务实施计划推进，先做数据库和纯后端服务，再接编排、声音来源匹配和前端。
- 新增规划服务可以直接读取 `chapter_analyses.structured_json`，无需解析渲染 Markdown；
 轻量目录天然可排除知识资产 ID、段落索引和长论据。
- 章节键按当前纳入拆书的一级章节顺序生成，并在每次运行的目录产物中保存键到
  `section_id` 的映射，因此模型不接触数据库 ID，后端仍可准确恢复来源。
- 《当代中国社会分层》真实 Kimi K3 验收中，25 章轻量目录为 12,171 字符，模块模型
  生成 12 个模块并完整覆盖 `CHAPTER_001` 至 `CHAPTER_025`。
- 首模块跨两个一级章节，实际模块材料只有 413 字符；Kimi 生成 5 集合法 Markdown，
  每集只含章节标识，内部结构化调用随后成功恢复 5 条页面数据。
- 真实验收三段调用总耗时约 9 分钟，说明 Kimi 路由可用但延迟较高；模块即时持久化、
  两路并发和单模块重跑是必要的产品能力。

## Phase 30 Diagnostics

- 前端只在页面首次挂载时读取一次 `/api/runs`，运行记录页没有持续轮询。
- `busy`、当前书籍、当前项目和批次对象都只保存在 React 内存中，刷新页面后立即丢失。
- 专辑大纲接口会在 HTTP 请求中同步等待完整模型流程，没有创建顶层持久运行；刷新或断开连接时，该协程可能被取消且无法恢复。
- 逐章拆书虽然创建父子运行记录，但接口仍同步等待整批完成；启动恢复逻辑没有覆盖 `book_analysis_batch` 和章节子任务。
- 单条声音和整张专辑批次已有后台运行记录与部分重启恢复能力，但前端轮询依赖当前页面内存中的选择状态，刷新后不会自动恢复任务面板。
- 当前模型供应商统一使用非流式请求，只能在一次模型调用完整结束后获得结果；若要逐字显示输出，需要扩展供应商流式协议、服务端事件通道和断线续传。
- 当前最小可靠修复边界应至少包括：所有长任务先持久化再返回、后台执行、统一进度字段、全局活动任务轮询、刷新后重新发现任务、启动时恢复所有受支持任务，以及阶段产物完成即展示。
- 用户确认首版采用阶段级展示：运行中显示当前步骤、进度和耗时；每个模型调用完成后立即展示该阶段完整结果；不要求逐字流式输出。
- `workflow_runs` 当前只有通用状态、父任务、位置和 JSON 元数据，适合通过幂等迁移新增显式进度、阶段、时间戳和重试次数字段。
- 应用已有 `active_tasks` 集合和 `create_task` 包装，但只保存协程引用，不按运行 ID 去重，也没有统一异常落库、关闭处理或恢复分派。
- 专辑生成已经把思维导图写入版本表，并把未校验专辑 JSON 写入项目草稿字段；两者都可直接作为阶段检查点复用，不需要新增内容表。
- 拆书当前在工作流内部创建父子运行，若路由先创建持久父任务，就必须允许工作流接收并复用该父任务 ID，避免创建两套父记录。
- 声音工作流每个阶段已经即时保存 `artifact_versions`，主要缺口是让运行记录在阶段保存后同步更新进度和输出引用。
- 批量服务已经按父任务和声音子任务持久化，可在不改变并发模型的前提下补齐显式进度字段、阶段回调与完成时间。
- 现有 `Database` 每次操作打开独立 SQLite 连接并自动提交，没有显式多语句事务 API；阶段结果与运行状态要通过可重入写入和产物存在性校验保证恢复安全。
- `WorkflowService.generate_episode` 已按 `outline/draft/final` 循环并在每一轮调用 `_save_artifact`，适合增加可选阶段回调而不改变现有测试和直接服务调用。
- 书籍 API 目前直接等待 `analyze_book`/`retry_chapter`，项目 API 直接等待 `generate_project_knowledge_outputs`；这三个入口是必须改为 202 后台任务的同步瓶颈。
- 叙事类旧拆书路径不是章节模型批次，仍应纳入顶层 `book_analysis_batch` 任务，但阶段进度只能按准备、人物关系/知识整理和完成展示。
- 前端 `Home` 已集中管理所有视图、选中对象、运行记录和网络请求，适合在根层增加唯一的活动任务轮询，而无需引入新的状态库。
- 当前单条声音按钮在 `runAction` 内最多轮询约 120 秒，导致按钮一直占用且刷新即丢失；改造后创建任务即结束 `runAction`，后续由全局轮询更新。
- 项目工作区已有批量进度卡，书籍工作区已有阶段条；可复用视觉语言增加统一 `TaskProgressCard`，并保留批量子任务明细。
- 后端测试以临时 SQLite 和可注入的假工作流/供应商为主，适合先增加运行服务单元测试，再逐步更新工作流与 API 契约。
- 模型路由已有 `snapshot_for_run` 和声音三阶段恢复方法；项目生成可直接按元数据中的 `mind_map`、`album_outline` 模型 ID 分别恢复，无需改变模型目录。
- `retry_chapter` 最终复用 `_analyze_non_narrative`，可把单章 ID作为父任务元数据输入，由同一个书籍后台执行器分派。
- 运行记录页面目前把除拆书外的所有任务都标成声音任务，后续需要增加项目知识生成标签、当前阶段与进度条，并移除“不因关闭网页丢失”但实际无轮询的静态宣称。
- 根组件已有统一 `refresh()`，但活动任务适合单独高频刷新，避免每秒同时请求完整书籍、项目、设置和全部历史运行记录。
- `ModelSnapshot.run_model_id` 已把环境模型规范化为可恢复的 `__environment__`，所有新持久任务元数据应只保存该稳定 ID，不保存供应商对象。
- `main.py` 中后台启动点只有启动恢复、批量生成和单条声音三个位置，改为 `TaskRegistry.spawn(run_id, factory)` 后即可统一去重与异常守护。
- 非叙事拆书的章节保存本身已在 `_save_chapter_analysis` 内使用一个显式 SQLite 事务，章节产物和知识资产具有可靠的原子边界；改造重点是复用父子运行记录和在章节完成后更新父进度。
- 全书拆书重启恢复时必须优先检查同一 fragment set 下的最新章节版本：若产物已成功但进程在更新子运行前退出，应把子运行补记成功而不是再次调用模型。
- 非流式模型请求无法在网络层立即中止；各工作流已增加取消谓词，在模型返回后、写入产物前再次检查，保证用户取消后不保存迟到结果。
- 项目生成的输入压缩本身可能包含模型调用，因此取消检查同时放在拆书稿准备完成后和两个主模型返回后。
- 顶层长任务必须在开始模型调用前持久化运行记录并立即返回 202；浏览器刷新只应丢失内存中的请求状态，不能丢失任务身份。
- 阶段结果无需逐字流式输出：每个模型阶段成功落库后保存稳定产物引用，页面再通过输出接口读取完整内容，既可恢复又不会暴露原书输入快照。
- 项目规划适合拆成“准备拆书稿、生成思维导图、生成专辑大纲、保存大纲”四个阶段；声音生产天然对应细纲、初稿、终稿三个阶段。
- 应用启动恢复必须沿用运行记录中锁定的模型与提示词版本；若产物已落库但阶段状态未写回，可通过最新版本对账补齐检查点，避免重复付费调用。
- 全局任务轮询只读取轻量运行列表；书籍、项目和声音详情仅在相关任务状态变化时刷新，避免每秒重复拉取大对象。
- 浏览器实测证明工作区与任务可以跨刷新恢复：运行记录页、当前阶段、50% 进度、已运行时间和已完成思维导图输出都能重新出现，控制台没有错误。

## Requirements

- 本地优先的网页产品，前后端边界清晰并预留上云能力。
- 输入支持 EPUB、TXT 和 Markdown。
- 两个人工检查点：章节切分、专辑大纲。
- 拆书、知识资产和思维导图属于知识库。
- 内容项目、声音细纲、初稿和终稿属于创作库。
- 首版界面打通单书闭环，底层允许未来项目关联多本书。
- 模型直接由产品调用，不再依赖 Dify。
- 任意声音支持指定节点重跑并保留版本。
- Obsidian 结构化沉淀，不读取旧笔记参与生成。
- 《圆圈正义》是端到端标准验收样书，原书不提交 Git。
- 全站改为 Notion 工作区式暖白、浅灰和克制紫色视觉。
- 内容项目使用三栏审核台，终稿为主编辑区，细纲与初稿默认折叠。
- 专辑大纲确认后由用户主动点击“生成全部终稿”。
- 每条声音内部依次生成细纲、初稿和口语化终稿；不同声音最多并行 5 条。
- 单条失败不阻断批次，失败项可从失败阶段重跑。
- 人工修改终稿保存为新版本，不覆盖模型原稿。
- 真实模型通过 DeepGate Anthropic Messages 路由调用。
- Anthropic 系统要求和任务正文必须合并进 user 消息。
- 公司 `.local` 路由使用 `httpx` 时必须设置 `trust_env=False`。
- 本机 DeepGate Messages 最小真实调用成功，目标供应商与模型返回非空文本。
- 单条声音当前使用 `source_section_ids` 绑定原文章节，但生成链没有构造统一的结构化上下文。
- 当前细纲输入只有绑定原文；初稿输入只有最新细纲；终稿输入只有最新初稿。
- 用户提供的声音细纲提示词要求同时提供书名、作者、人物关系、当前声音内容框架和对应书籍内容。
- 用户确认细纲输入为当前声音框架与原文块，初稿输入为细纲与原文块，终稿输入为初稿与原文块。
- 当前 `episodes` 表没有独立的声音框架正文列，专辑审核页只能编辑标题、内容类型、风格和来源小节。
- 书名与作者可从 `books` 表取得；人物关系尚未作为独立知识资产写入当前数据库。
- 每条声音将新增可编辑的多行框架字段，确认专辑大纲时一并审核，生成细纲时原样传入。
- 书籍上传时由用户明确选择叙事类或非叙事类；人物关系只进入叙事类的提示词与输入快照。
- 叙事类人物关系由拆书模型提取并存为 `人物关系` 知识资产，使用 `source_section_ids` 关联原文块；细纲阶段不得读取无关块的人物关系。
- `books` 与 `episodes` 均支持通过现有 `MIGRATION_COLUMNS` 为旧 SQLite 增加带默认值的新列。
- 书籍上传使用 multipart 表单，新增 `book_type` 可沿用 `Form` 参数，不需要改变文件传输方式。
- 专辑大纲保存目前通过删除并重建 `episodes` 实现，新增声音框架必须一同写回，否则会在保存时丢失。
- 当前 `analyze_book` 是同步方法和同步 API，真实模型供应商的 `generate` 是异步方法；叙事类人物关系提取需将拆书调用链调整为异步。
- 前端书籍与声音 TypeScript 类型、上传表单、书籍工作台和 `OutlineEditor` 是本次主要界面改动点。
- `DemoProvider` 按 `prompt.id` 返回确定性内容，新增人物关系提示词需要对应演示响应，才能在无真实密钥的测试中覆盖叙事拆书。
- 现有后端测试直接调用同步 `analyze_book`，改为异步后需更新为 `asyncio.run` 并新增输入捕获供应商。
- 当前没有 FastAPI 端点级测试，接口校验可以通过轻量 `TestClient` 新用例补齐，也可优先测试服务层和 Pydantic 模型。
- 前端结构测试读取 `app/page.tsx` 与 CSS 源码，适合补充书籍类型选择和声音框架编辑器的静态断言。
- 输入快照保存在本地 SQLite 供追溯，但声音详情 API 显式排除该列，避免把完整原文上下文发送到浏览器。
- 当前 DeepGate Anthropic 对人物关系新提示词返回合法 JSON，短叙事样例得到 1 条关系。
- 现有本地 SQLite 已成功迁移，新旧书籍均默认得到 `non_narrative`，既有声音保留并回填可用框架初值。
- 已越过专辑大纲审核的历史声音无法再编辑空框架，因此迁移时为历史空值按声音标题生成可用初值；新项目仍由专辑大纲流程生成和审核框架。
- 用户要求非叙事类书籍根据已确认的章节切分逐章调用模型，输出章节主题、子主题、内容索引、概念定义、金句、主要观点、论据和案例故事。
- 《当代中国社会分层》EPUB 导入成功，状态曾被标记为 `analyzed`，但 `knowledge_items` 数量为 0。
- 该书共有 234 个已确认节点：30 个一级、99 个二级、105 个三级标题，没有四级标题；现有 EPUB 解析器将所有 1–3 级节点标为 `theme`。
- 当前 `analyze_book` 只处理 `kind IN ('article', 'section')`，因此对该书得到空输入，并在未调用模型的情况下错误完成。
- 现有 `book_analysis` 提示词只是一段简略占位内容，非叙事类知识资产仍由本地句子截取生成，不符合用户要求的逐章模型拆书。
- 目标书籍按一级节点聚合后，主要章节长度约为 3,591–26,642 字；目录为 483 字。
- EPUB 中有 4 个表格说明/脚注被 XHTML 的一级标题样式误识别为独立一级章节，若直接遍历所有一级节点会产生垃圾模型调用。
- 当前 React 章节审核器硬编码只展示三级和四级节点，并只允许在这两级间调整，用户实际上无法审查该 EPUB 的一级章节边界。
- 一级章节候选需要保存独立的“是否纳入拆书”状态，不能继续复用会被批量覆盖的 `sections.status`。
- 完整章节拆书稿需要版本化保存；派生知识卡片必须能追溯到章节分析版本，重跑时只替换对应章节的模型资产。
- 逐章拆书应使用项目级父运行与章节子运行，最多 5 章并行并支持部分失败重跑。
- 完整拆书稿是思维导图和专辑大纲的共同事实输入，压缩时必须保留 `content_index`、章节归属、概念、观点、关键论据、案例和金句。
- 专辑大纲的 `section_identifier` 必须可确定性映射回拆书稿 `content_index` 和原文块 ID，不能只保存模型生成的自由文本索引。
- 专辑大纲不再要求固定 30 条以上；生成前提供可选的“专辑特殊要求”和“期望集数”输入。
- 用户未填写期望集数时，由模型按完整或压缩后的拆书稿内容量自行决定集数。
- 用户填写期望集数时，将其作为明确的创作目标传给模型，但仍不得拆分、伪造或丢失 `content_index` 来机械凑数；若内容索引与目标集数无法同时满足，优先保证来源可追溯并返回可见提示。
- 模型可以根据全书主要内容、特殊要求和期望集数选择进入专辑的 `content_index`；未选索引仍保留在完整拆书稿和知识库中。进入专辑的普通索引不得被拆分到多条声音。
- 过渡声音是模型可选能力，仅在知识模块之间确有必要时增加，不强制生成，也不得用于填充集数。
- 用户确认模型以严格 JSON 生成专辑大纲，后端负责结构、来源索引和集数约束校验。
- 思维导图和专辑大纲使用同一份完整或压缩拆书稿并行生成；专辑特殊要求与期望集数只附加到专辑大纲调用。
- 当前上传控件是未受控的原生 `<input type="file">`，只在提交时从 `FormData` 读取文件；选择文件后页面中的“选择 EPUB、TXT 或 Markdown”文案、图标和按钮均不变化，因此用户无法确认选择是否生效。
- 全局模型继续作为默认值；每个内容项目可分别覆盖章节拆书、思维导图、专辑大纲、声音细纲、声音初稿和声音终稿。
- 专辑大纲默认使用 `kimi-k3`，其他环节默认跟随当前全局模型。
- JSON 修复继承原任务模型，人物关系提取继承章节拆书模型，长文本压缩继承对应的思维导图或专辑大纲模型，不增加独立选择框。
- 项目级模型选择在任务启动时解析并锁定快照，运行中修改只影响下一次任务。
- 专辑大纲提示词需围绕核心问题组稿，标题灵活使用问题、冲突、悬念、反常识与因果追问；每集主要内容明确输出核心主题和两至四条核心要点。
- 新一轮专辑大纲优化明确默认听众未读原书并主要通过音频理解内容；需要补充相邻声音知识承接、最低必要背景解释和全专辑认知路线。
- 风格采用“轻松但克制”：借鉴高信息密度故事讲述的因果链、反差和适度冷幽默等高层特征，不复制特定创作者的口头禅、标志性句式或个人表达。
- 现有专辑大纲结构校验只要求 `核心主题` 与 `核心要点`，演示供应商也只输出这两段；本轮需要增加 `听众钩子` 并校验至少两条核心要点。
- 专辑大纲生成结果最终把 `main_points` 原样保存为声音的 `content_framework`，因此新增听众钩子无需数据库迁移或前端字段改造。
- 前端源码位于项目根目录 `app/`，不是 `frontend/src/`；本轮后端结构变化能通过现有三栏审核台直接展示。
- `_validate_album_outline` 已按知识资产权威来源重建 `section_identifier` 和 `source_content_indexes`，本轮只需扩展 `main_points` 的确定性结构校验，不应改动溯源逻辑。
- 专辑草稿缓存签名已经包含专辑提示词版本、模型和完整输入；提示词升级到 `2026-07-28.3` 后不会误复用旧结构草稿。
- 已确认存在声音的项目不会因提示词升级自动改写；只有用户主动重新生成专辑大纲时才会运行新提示词并保存新结果。
- 提示词配置采用“全局默认 + 项目级覆盖”，四个环节统一放在左侧新“提示词”标签页。
- 用户只编辑用户提示词主体；系统角色、输出结构、知识资产与原文索引约束由程序保护。
- 提示词保存时必须校验必要占位符与未知占位符，每次保存形成不可变历史版本。
- 提示词需要模板、版本和项目绑定三类数据边界；任务启动时解析并锁定不可变快照，才能与现有模型快照规则保持一致。
- 现有 `Database.init()` 采用幂等建表加缺列迁移，适合直接加入提示词三表和 `artifact_versions.prompt_version_id`，无需单独迁移框架。
- 现有 `EpisodeContextBuilder` 把书籍信息、声音框架、人物关系、上一步产物和原文证据拼成单一 `source`；需要扩展为结构化变量字典，同时保留旧 `source` 供不可配置环节和兼容测试使用。
- 专辑生成当前直接调用代码中的 `PROMPTS["album_outline"]`，声音生成也按 `context.prompt_id` 读取静态定义；提示词服务应输出临时 `PromptDefinition` 与版本快照，以最小范围复用现有 provider 接口。
- 批量任务已在 `workflow_runs.metadata_json` 保存三阶段模型 ID，可在同一元数据中并列保存三阶段提示词快照 ID，复用现有重启恢复边界。
- `WorkflowService.generate_episode` 是保存声音产物的单一入口，适合新增可选 `stage_prompt_snapshots` 参数；`_save_artifact` 可同步写入提示词版本 ID。
- 专辑大纲是同步接口调用，可在路由入口解析一个提示词快照后传给工作流；专辑草稿签名必须改用该快照的系统/用户版本 ID。
- 统一启动恢复会重新调用 `execute_episode_run` 或 `BatchService.run_batch`，因此恢复路径必须从运行元数据读取提示词版本 ID，不能只在内存传递。
- 前端是单文件 `app/page.tsx`，主导航目前四项；新增 `prompts` View、页面组件和独立 CSS 区块即可保持现有页面结构。
- 移动端主导航当前固定四列，增加第五个标签后需改为自适应或五列，避免提示词入口换行错位。
- 专辑提示词快照可以保持 provider 的 `prompt.id=album_outline`，同时把编译后的完整用户消息作为 `{source}` 传入，因此现有 JSON 输出参数和演示供应商分支无需改写。
- 声音上下文扩展为变量字典后，原有 `source` 仍被保留，现有上下文与工作流测试可保持兼容。
- 提示词预览可复用声音 outline 上下文得到书籍、框架、原文和人物关系，再补充已有细纲/初稿；阶段产物缺失时使用明确示例，不需要调用模型。
- API 测试已覆盖全局版本、项目继承与覆盖、保存校验、历史、预览、取消项目覆盖和恢复系统默认。
- 现有 CSS 没有可复用的空状态、文本危险按钮或提示词工作台类，需要为新三栏页提供独立样式，同时沿用现有颜色、圆角和按钮变量。
- 锁定版本恢复必须同时校验模板 ID、系统作用域和项目归属，防止将其他环节或其他项目的版本 ID 混入当前任务。
- 设置页切换后立即生效；已启动任务继续使用启动时锁定的模型，新任务使用新模型。
- 保留现有 Claude Sonnet 4.6，并新增 `kimi-k3`、`claude-sonnet-5`、`glm-5.2`、`kimi-k2.6`、`deepseek-v4-pro` 和 `hy3`。
- 模型预设使用共同的本机 API Key；浏览器只提交预设 ID，不能提交任意 Base URL。
- DeepGate Base URL 以 `/api/v1` 结尾时应追加 `/messages`，以 `/api` 结尾时应追加 `/v1/messages`。
- 全局选择保存在本地数据目录，不能进入 Git；API Key 仍只从环境变量读取。
- 当前 `main.py` 在模块加载时构造单例 `provider`、`WorkflowService` 和 `BatchService`；所有路由与后台恢复任务共享该对象。
- 当前 `WorkflowService` 的章节拆书、压缩、思维导图、专辑大纲、JSON 修复和声音三阶段均直接读取 `self.provider`，若简单替换该字段会让运行任务中途换模型。
- `BatchService.create_batch` 与后台 `run_batch` 分离，重启恢复依赖数据库运行记录；要保持运行任务模型一致，需要在运行元数据中保存非敏感模型 ID，并在恢复时重建相同 provider。
- 单条声音运行也先写 `workflow_runs` 再启动后台协程；可以用同一模型 ID 元数据实现任务快照和重启恢复。
- 章节拆书与专辑大纲当前是请求内等待完成的顶层异步任务；路由入口可捕获 provider 并显式传给工作流。
- 设置页已经集中读取 `/api/settings/status`，适合扩展当前模型 ID、来源和预设列表，不需要新增前端配置数据源。
- 运行中的真实本地后端已公开 7 个模型，当前默认仍为 Claude Sonnet 4.6，API Key 状态只返回布尔值。
- 浏览器验收确认设置页显示全部 7 个选项、当前模型、全局保存按钮和“运行中任务不受影响”说明。
- 环境变量无法匹配预设时，运行记录使用内部 `__environment__` 快照 ID；即使之后切换全局模型，重启恢复仍能重建原环境 provider。
- 思维导图表补充 provider/model 字段，新增结果可与章节拆书稿和声音版本一样追溯实际模型。
- 用户确认原书一级章节结构与逐章调用边界保持不变，段落切分只发生在标题节点正文内部。
- 原文块采用约 300–800 字语义段落：短段合并、长段按句切分，不跨标题节点。
- 每项概念、金句、观点、论据和案例分别保存有序 `source_content_indexes`，允许多个来源。
- 所有知识资产必须引用真实段落索引；金句与概念、观点、论据、案例和案例证据一样允许忠实整理，不再执行逐字原文匹配。
- 旧拆书版本和知识资产保留为旧索引版本，不自动猜测精确来源；旧书需逐章重跑。
- 专辑大纲改按知识资产组合声音，段落索引只负责证据定位。
- 声音组稿输入包含直接证据和同标题节点相邻辅助上下文，两者必须明确分区。
- 新专辑生成前，所有纳入拆书的章节必须完成精确溯源版本，避免静默漏章。

## Research Findings

### 2026-07-29 示例字幕初步观察

- 六篇字幕均不是从定义或章节目录开场，而是先用反常识事实、现实困惑、巨大数字或听众熟悉的生活场景制造“认知缺口”，随后明确本期要串起的核心问题。
- 开场常见结构是：具体钩子 → 连续追问 → 降低承诺（“先挑主干”“尽量轻松聊”）→ 给出认知路线；听众在进入概念解释前先知道“为什么值得听”和“听完会明白什么”。
- 抽象概念通常通过一个极简假设或可复用的小模型解释，例如把利率先简化为全经济体同一利率、用奶茶店股权稀释解释上市、用房子和包区分消费品与投资品。
- 信息推进主要依赖连续因果链：“如果 A → 主体会做 B → B 传导到 C → 最终出现 D”，而不是罗列观点。连接词和短问句承担导航作用。
- 语气轻松但信息并不稀释：口语填充词、轻微自嘲、生活化类比和短段调侃用于缓冲高密度解释，随后会迅速回到机制或事实；幽默不是独立段子。
- 常用“先简化、再加条件、最后纠偏”的教学顺序。先让听众抓住主干，再补风险、例外、历史背景和现实复杂性。
- 数据与案例通常先解释量级或意义，再进入结论；重要术语第一次出现会立刻翻译成日常语言。
- 现有专辑大纲模板已经覆盖未读听众、单集问题、因果链和轻松克制，但细纲、初稿、终稿仍主要强调准确和口语化，缺少可执行的开场钩子、认知缺口、极简模型、因果推进、难度梯度、信息密度和收束回扣规则。
- 现有终稿保护规则“不得增加初稿未覆盖的新事实”合理，意味着关键的讲述设计必须在细纲阶段确定、在初稿阶段写足，终稿不能承担结构补救。
- 中段推进高度依赖“你看、比如、假设、所以、但是、再来看”等听觉路标。六篇中“所以”约 16–35 次、“比如”约 9–20 次，说明其核心不是辞藻，而是反复把听众带回因果关系和具体例子。
- 结尾通常回扣开头问题，并把已讲内容压缩成一个可复述的框架或判断；不会重新摘要每一段。房地产稿回到“个人是否买房”的生活尺度，游戏稿回到“巨头为何入局”，通胀稿回到“预期为何影响政策”。
- 复杂主题会主动标出边界和不确定性，例如利率对资产价格的影响不是单向、房地产指标可能同时出现在泡沫中。它会先给主干，再用“但是/不过”纠正过度简化，避免把通俗化写成绝对化。
- 案例有两种职责：奶茶店等虚拟案例用于解释机制，蒙牛/俏江南等真实案例用于展示机制如何改变结果。提示词应要求模型先判断案例职责，不要只因“有趣”而堆案例。
- 四阶段职责应明确：专辑大纲确定全局认知路线；声音细纲设计本集认知缺口、极简模型、因果节点和纠偏点；初稿完成事实与解释；终稿只做听觉化、减书面腔和节奏控制。

### “城乡分野为何如此顽固”样稿事实边界

- 样稿使用两个段落级原文块：`content_05ff08770b509d6a8578`、`content_5eaa58c512da25730a7b`，均属于《当代中国社会分层》第二章“二、城乡分野”。
- 原书主线不是“户籍制度创造了城乡差异”，而是：城乡资源配置不平衡历史久远 → 古代“国人/野人”和城墙强化空间、利益区隔 → 1950 年代中后期严格户籍制度进一步固化 → 改革开放后人口可以流动，但身份和资源转换仍明显滞后。
- 可用的具体抓手包括：“城墙”这一历史意象、1959–1979 年城乡人口流动基本终止、“招工”被称作“鲤鱼跳龙门”、2 亿多农民工尚未真正完成市民身份转换、全国城乡 PPS 抽样调查中的 27.6% 与 20 年 7.7% 数据。
- 需要谨慎表达：27.6% 在原文中的措辞是“目前农村户籍人口真正转变为城镇户籍人口的比例……仅占全国总人口的 27.6%”，不应擅自改写为全部农村人口的转户率。
- 对应知识资产为：观点 `knowledge_5dd6901b1ae83b0d9dd198df`，案例 `knowledge_af3c9c9718b4d5d451a80af9`，论据 `knowledge_c5ca49ed5acfefe4dd86a1fa`、`knowledge_cfd9f41bd8bb9449b0c42ec3`、`knowledge_8151850702d636a339948b58`，金句 `knowledge_5ce9a7136e9205c83b86910e`。
- 这条样稿适合采用“常见误解：城乡差异由 1950 年代户籍制度造成 → 历史反证：国人/野人和城墙 → 制度加固：1959–1979 年 → 当代余波：能进城不等于成为市民”的认知路线。
- 用户要求口播正文以事实和逻辑为主，不反复提作者；调查直接说明调查主体与结果。仅在直接引用、观点有争议或需要区分不同立场时保留归因。
- 用户确认采用分层约束：事实与范围安全 > 声音框架与细纲结构 > 听众理解 > 表达与趣味 > 归因细节。模型不得使用自身知识补充新闻；原文没有金句时不强行引用。
- 细纲预告、跨模块过渡句和结尾作为锁定句进入初稿与终稿；“跨模块只用锁定过渡句”不限制模块内部必要的因果连接语。
- 四阶段分层模板已发布为本地全局 v1：专辑大纲 `18f515a1a5a04a1bb3dbf4e3b81e0523`、声音细纲 `072227f1165845fe87964b45774bb85e`、声音初稿 `3554791e21b24ac5bbcd23a02788d492`、声音终稿 `8cace38f516e4ffbb0bd985c8fb7218b`。
- 发布前四阶段都使用系统默认且没有全局覆盖；系统默认版本仍保留，可在提示词页面恢复。当前数据库没有项目级提示词覆盖项，发布过程没有删除任何项目绑定。
- 使用《当代中国社会分层》专辑 1 的真实项目与声音上下文预览四个模板，占位符全部替换且保护约束存在。预览显示截断是页面对长原文的 6000 字预览保护，不影响实际任务输入。

- 当前章节拆书 API、专辑生成 API、单条声音和整张专辑批次都在路由入口获取一个全局模型快照。
- `WorkflowService.generate_project_knowledge_outputs` 当前让思维导图、专辑大纲、专辑 JSON 修复和必要压缩共用同一个 provider，需要拆成 mind-map 与 album-outline 两套快照。
- `WorkflowService.generate_episode` 当前用一个 provider 连续执行细纲、初稿和终稿，必须改为按 stage 解析 provider，同时保持一次运行的三个阶段快照固定。
- `BatchService` 当前只在父批次元数据保存一个 `model_id`，重启恢复依赖该值；新结构需要保存 `stage_model_ids`，子任务运行时按阶段获取对应 provider。
- 项目创建目前只写基础字段，适合在 `projects.model_overrides_json` 中写入专辑大纲默认 `kimi-k3`。
- 项目详情和列表的数据库 JSON 字段会由通用数据库读取层自动解码，可直接向前端公开规范化后的 `model_overrides` 和 `effective_models`。
- 项目工作台已有专辑生成卡和声音批量卡，适合加入实际模型摘要；六项完整配置放在独立可折叠卡中。

- 现有需求把流程划分为书籍切分、拆书稿、思维导图、专辑大纲、声音细纲、声音初稿和文本风格调整。
- 现有 YAML 包含 LLM、代码、条件、迭代、HTTP/RPC 等节点；本地文件入库可替代远程书籍 HTTP/RPC。
- 声音类型包含解读、过渡和故事；风格包含观点和鸡汤；文本精修还区分是否有前序声音。
- 《圆圈正义》为 UTF-8 Markdown，约 37 万字节，8 个三级标题、49 个四级标题、1,286 个字面量换行和大量脚注标记。
- 《圆圈正义》的正文行极长，解析必须先规范换行，再进行标题树和语义切分。
- Sites 标准工程已经成功初始化，使用 vinext/Vite 结构；本地预览地址为 `http://localhost:3000/`。
- 初始化器自带 Cloudflare 兼容构建结构，但本项目首版只做本地交付。
- 系统 Python 中已有 FastAPI，但其 `pydantic_core` 是 x86_64，与当前 arm64e 进程不兼容，不能复用。
- 后端将使用项目私有 `.venv`，避免污染或依赖系统 Python。
- 《圆圈正义》真实 API 导入得到 62 个节点，其中 8 个主题、49 篇文章和 4 个长文语义分段。
- 演示拆书生成 102 条知识资产：49 个观点、49 条金句和 4 个案例；思维导图 56 行。
- 内容项目生成 12 条声音；第一条声音完成细纲、初稿和终稿，并从初稿节点重跑得到 draft v1/v2 与 final v1/v2。
- 第一次 Obsidian 同步写入 171 个内容文件和 1 个清单文件；第二次同步变更数为 0。
- 持久任务接口记录 pending/running/succeeded/failed/cancelled，并在服务启动时恢复未完成任务。
- 新批次协调器以声音为并发单位，单条声音占用一个槽位并依次完成三个阶段。
- 《圆圈正义》已有 2 条声音终稿；批次自动跳过后，成功生成剩余 10 条，最终 12 条均可审核。
- 人工终稿保存得到 `author_type=human` 的 v2，同时模型 v1 保持不变。

## Technical Decisions

| Decision | Rationale |
|----------|-----------|
| SQLite 为主数据，Obsidian 为知识视图 | 运行状态和版本需要事务一致性，Vault 需要可读性 |
| 节点运行记录输入快照、提示词版本和模型配置 | 支持追溯、重跑和版本对比 |
| 模型供应商统一适配接口 | 可替换 Claude、Gemini、豆包、通义、DeepSeek 或 OpenAI 兼容端点 |
| 进程内持久队列 | 首版无需 Redis，状态写入 SQLite，重启可恢复 |
| 系统生成区块与人工批注区块分离 | 增量同步不覆盖个人内容 |
| 后端负责文件与 Vault 访问 | 浏览器不直接获得任意本地文件权限 |
| 项目父运行 + 声音子运行 | 支持整张专辑汇总、单条失败隔离和重启恢复 |
| `author_type` 区分模型与人工 | 终稿审校不覆盖模型产物 |
| Anthropic 使用原生 `httpx` 适配器 | 复用现有依赖并精确控制 DeepGate 请求 |
| 实际访问密钥只保存在 Git 忽略的 `.env` | 防止配置进入版本历史、示例文件或前端 |
| 一级章节与段落证据采用双层结构 | 章节仍是审核和模型调用边界，段落块只承担精确引用与组稿 |
| 片段 ID 由书籍、标题节点、规范文本和重复序号生成 | 同一正文可稳定复用，正文变化则进入新的片段集版本 |
| 知识资产 ID 由类型、正文和有序来源确定 | 章节重跑后相同资产保持身份，来源变化时生成新资产 |
| 所有知识资产只做结构和来源索引校验 | 保持段落级溯源，同时避免标点、摘录范围或轻微措辞调整阻断整章 |
| 专辑按知识资产编排、段落索引只作证据 | 为未来一篇稿件融合多本书留下清晰扩展边界 |
| Doubao Seed 2.0 Pro 使用 OpenAI 兼容适配器 | 用户确认的端点是 `/chat/completions`，无需新增专用 provider |
| 结构化拆书失败需区分截断、JSON 语法和证据校验 | 三类问题的恢复方式不同，不能统一交给 JSON 修复 |
| 部分成功章节保存有效资产但阻断下游 | 避免单项错误拖垮整章，同时防止不完整内容静默进入专辑 |
| 项目级模型覆盖使用稳定环节键保存 | 让全局默认与六环节覆盖可以独立演进，并避免修改一个环节覆盖其他配置 |
| 辅助模型任务继承所属内容任务 | 不把 JSON 修复、人物关系和压缩等技术细节暴露成额外配置负担 |

## Issues Encountered

| Issue | Resolution |
|-------|------------|
| 当前父工作区已有大量无关改动 | 新建独立嵌套 Git 项目，所有实现只写入 `ai-book-studio` |
| 原始资料位于项目外 | 只读解析并记录来源，必要的提示词迁移进项目，原书不复制 |
| 无法在自动化测试中使用付费模型 | 增加确定性演示供应商，真实模型使用同一契约 |
| 站点初始化器要求空目录 | 临时移出已提交文档，初始化后恢复并保留 Git 历史 |
| npm 与开发服务器需要沙箱外权限 | 按权限流程完成安装和本地监听 |
| 系统 FastAPI/Pydantic 二进制架构不匹配 | 使用项目私有虚拟环境安装正确架构的依赖 |
| React effect lint 检测到同步 setState | 初始请求改用 Promise 回调，编辑器保留本地草稿而不做同步 effect |

## Resources

- 设计规格：`docs/superpowers/specs/2026-07-24-ai-book-studio-design.md`
- 需求：`/Users/xmly/Downloads/AI讲书批量化流程需求.md`
- SOP：`/Users/xmly/Downloads/就书讲书sop.md`
- Dify 工作流：`/Users/xmly/Downloads/【就书讲书】*.yml`
- 验收样书：`/Users/xmly/Downloads/拆书/圆圈正义.md`

## Visual/Browser Findings

- 已确认“知识库 / 创作库”双域，而不是“一本书一个封闭工作台”。
- 书籍库展示拆解状态和知识资产数量。
- 创作库展示内容项目、项目来源包、专辑大纲和声音生产状态。
- 声音工作台需要同时呈现细纲/初稿/终稿、引用证据和版本重跑。
- Obsidian 目录分为总览、书籍知识资产、内容项目和同步清单。
- 用户在视觉伴随中选择“三栏审核台”，而非 Notion 文档页或沉浸式单栏。
- 三栏分别承载专辑目录与批次、中间终稿编辑、右侧证据与版本。
